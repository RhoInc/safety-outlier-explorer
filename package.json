{
  "name": "se-outlier-explorer",
  "version": "0.0.0",
  "description": "outlier explorer for safety explorer",
  "main": "renderer.common.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-watch": "cd test && node watcher.js",
    "build": "rollup -c | babel > index.js",
    "build-renderer": "rollup -c renderer.rollup.config.js | babel > renderer.common.js"
  },
  "author": "Rho, Inc.",
  "license": "ISC",
  "dependencies": {
    "d3": "^3.5.14",
    "react": "^0.14.7",
    "webcharts": "^1.3.2"
  },
  "devDependencies": {
    "babel": "^5.8.35",
    "rollup": "^0.25.1"
  }
}
